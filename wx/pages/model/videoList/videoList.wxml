<block wx:for="{{allObj}}" wx:key="{{item.index}}">

    <view style="{{item.style}}" class="zj-video">
        <video src="{{item.vis}}" style="width:100%;height:100%;"></video>
    </view>

</block>